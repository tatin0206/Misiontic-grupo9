import React from "react";
import {Button } from 'react-bulma-components'

const AddButton = ({ onClick }) => {
    return (
       
    <Button onClick={onClick} color="primary" >Actualizar</Button>
             
    )
}



export default AddButton